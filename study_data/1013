 (this->*funPtrArr[i])()的意思
 
 (this->*funPtrArr[i])() 是一个 C++ 中的函数指针调用的语法。让我来解释一下它的意思。

首先，这是一个成员函数指针的调用方式，其中 funPtrArr 是一个指向成员函数的指针数组，而 this 是一个指向当前对象的指针。

接下来，我们使用箭头运算符 -> 来访问当前对象的成员函数。通过解引用 funPtrArr[i]，我们可以获取到第 i 个成员函数指针。

最后，我们使用圆括号 () 来调用这个成员函数指针，传递 this 指针作为参数。这样就可以调用该成员函数。

总结起来，(this->*funPtrArr[i])() 的意思是调用 funPtrArr 数组中第 i 个成员函数指针所指向的成员函数，并将当前对象的指针作为参数传递进去。
